apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-service
  namespace: yelp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: review-service
  template:
    metadata:
      labels:
        app: review-service
    spec:
      containers:
        - name: review-service
          image: yelp/review-service:latest
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: yelp-config
            - secretRef:
                name: yelp-secrets
          readinessProbe:
            httpGet:
              path: /
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 8004
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: yelp
spec:
  selector:
    app: review-service
  ports:
    - port: 8004
      targetPort: 8004
  type: ClusterIP
